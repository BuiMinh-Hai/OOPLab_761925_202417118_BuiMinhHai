Question:
- If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this?
Answer:
NO. Java does not allow you to do this because the two constructors have the same number of parameters and the same data type (String), resulting in a duplicate method signature error.